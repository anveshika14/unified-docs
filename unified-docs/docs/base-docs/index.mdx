---
title: 'Welcome'
mode: "custom"
---

export const Carousel = ({ children, autoPlay = true, interval = 5000 }) => {
  const [currentIndex, setCurrentIndex] = React.useState(0);
  const slides = React.Children.toArray(children);

  const nextSlide = () => setCurrentIndex(prev => (prev === slides.length - 1 ? 0 : prev + 1));
  const prevSlide = () => setCurrentIndex(prev => (prev === 0 ? slides.length - 1 : prev - 1));

  React.useEffect(() => {
    if (!autoPlay) return;
    const timer = setInterval(nextSlide, interval);
    return () => clearInterval(timer);
  }, [currentIndex]);

  return (
    <div style={{
    height: '530px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    textAlign: 'center',
    color: 'white',
  }} >
    <div style={{
      position: 'relative',
      width:'85%',
      margin: '2rem auto',
      overflow: 'hidden',
      borderRadius: '8px',
      boxShadow: '0 4px 12px rgba(0,0,0,0.1)',
      display:'flex',
      justifyContent:'center'
    }}>
      <div style={{
        display: 'flex',
        transform: `translateX(-${currentIndex * 100}%)`,
        transition: 'transform 0.5s ease'
      }}>
        {slides.map((slide, i) => (
          <div key={i} style={{ minWidth: '100%' }}>{slide}</div>
        ))}
      </div>

      <button onClick={prevSlide} style={{
        position: 'absolute',
        top: '50%',
        left: '10px',
        transform: 'translateY(-50%)',
        background: 'rgba(0,0,0,0.5)',
        color: 'white',
        border: 'none',
        borderRadius: '50%',
        width: '40px',
        height: '40px',
        cursor: 'pointer',
        fontSize: '20px'
      }}>‹</button>

      <button onClick={nextSlide} style={{
        position: 'absolute',
        top: '50%',
        right: '10px',
        transform: 'translateY(-50%)',
        background: 'rgba(0,0,0,0.5)',
        color: 'white',
        border: 'none',
        borderRadius: '50%',
        width: '40px',
        height: '40px',
        cursor: 'pointer',
        fontSize: '20px'
      }}>›</button>

      <div style={{
        position: 'absolute',
        bottom: '20px',
        left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex',
        gap: '8px'
      }}>
        {slides.map((_, i) => (
          <button
            key={i}
            onClick={() => setCurrentIndex(i)}
            style={{
              width: '12px',
              height: '12px',
              borderRadius: '50%',
              border: 'none',
              background: currentIndex === i ? '#333' : '#ccc',
              cursor: 'pointer',
              padding: 0
            }}
          />
        ))}
      </div>
    </div>
    </div>
  );
};


<Carousel>

  
  {/* Slide 1 */}
  <div style={{
    background: 'url(/images/iconss/image1.webp) no-repeat center center',
    backgroundSize: 'cover',
    color: 'white',
    padding: '80px 20px',
    textAlign: 'center',
    height: '400px',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems:'center'
  }}>
     <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
         <h2 style={{fontSize:'45px',marginTop:'10px'}}><span style={{
            background: 'linear-gradient(91deg, #ffbd12 0, #e03d3d 80%)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent'
         }}>**WELCOME &nbsp;TO**</span> </h2>
         <a href="https://enginify.in"> 
         <img src="/logo/enginify_logo.png" alt="icon"/></a>
         <h2 style={{fontSize:'45px',marginTop:'10px'}}><span style={{
            background: 'linear-gradient(91deg, #ffbd12 0, #e03d3d 80%)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent'
         }}>**DOCS**</span></h2>
    </div>

    <p style={{ margin: 0, fontSize: '18px'}}>
     Explore how to authenticate, connect, and integrate our APIs — the right way.
    </p>
    <a href="/docs/base-docs/guides" style={{ textDecoration: 'none' }}>
    <button className="started">
      Get started
    </button>
    </a>
  </div>

  {/* Slide 2 */}
  <div style={{
    background: 'url(https://ik.imagekit.io/neuwx10vxnmeo/enginify/images/bg-intro-about-us_41LnDHOZA.jpg?updatedAt=1718015717328) no-repeat center center',
    backgroundSize: 'cover',
    color: 'white',
    padding: '80px 20px',
    textAlign: 'center',
    height: '400px',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center'
  }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
         <h2 style={{fontSize:'45px',marginTop:'10px'}}>**Build and scale your business
fast with our**  
         <span style={{
            background: 'linear-gradient(91deg, #ffbd12 0, #e03d3d 80%)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent'
         }}>**world-class
tailored solutions**</span></h2>
    </div>
     <a href="/docs/base-docs/guides" style={{ textDecoration: 'none' }}>
    <button className="started">
      Let's Chat
    </button>
    </a>
  </div>


  {/* Slide 3 */}
  <div style={{
    background: 'url(https://picsum.photos/1220/400?random=1)',
    color: 'white',
    padding: '80px 20px',
    textAlign: 'center',
    height: '400px',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems:'center'
  }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
         <h2 style={{fontSize:'45px',marginTop:'10px'}}>**Want to know more about** <span style={{
            background: 'linear-gradient(91deg, #ffbd12 0, #e03d3d 80%)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent'
         }}> **ENGINIFY**</span>**?** </h2>
         

    </div>
      
     <a href="https://enginify.in/services" style={{ textDecoration: 'none' }}>
    <button className="started">
      About Us
    </button>
    </a>
  </div>

   {/* Slide 4 */}
  <div style={{
    background: 'url(https://picsum.photos/1220/400?random=2)',
    color: 'white',
    padding: '80px 20px',
    textAlign: 'center',
    height: '400px',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems:'center'
  }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
         <h2 style={{fontSize:'45px',marginTop:'10px'}}>**Want to know more about** <span style={{
            background: 'linear-gradient(91deg, #ffbd12 0, #e03d3d 80%)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent'
         }}> **ENGINIFY**</span>**?** </h2>
         

    </div>
      
     <a href="https://enginify.in/about" style={{ textDecoration: 'none' }}>
    <button className="started">
      About Us
    </button>
    </a>
  </div>
</Carousel>


<div style={{
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center'
}}>
     <CardGroup cols={3}>
       <Card>
         <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px'
      }}>
         <img src="/images/i11.svg"/>
        **Team Augmentation.** <br/>
         Enhance your team and \
         challenge the deadline
        </div>
       </Card>
       <Card>
         <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px'
      }}>
         <img src="/images/i12.svg"/>
        **Custom Software Development** <br/>
         Build software
tailored to your business
        </div>
       </Card>
       <Card>
         <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px'
      }}>
         <img src="/images/i13.svg"/>
        **AI Development** <br/>
         Disrupt
the market
        </div>
       </Card>
     </CardGroup>
</div>



<p className="text-center mt-12 mb-8 text-[25px] text-[#000000] dark:text-white">
   Explore the Docs
</p>

<div className="flex justify-center mb-30 " >
     <div style={{marginBottom:'70px'}}>
        <CardGroup cols={3}>
          <Card
             title="Quickstart"
             icon="play"
             color="#9935e8"
             href="/docs/base-docs/guides"
             arrow="true"
          >
             Start using our API in minutes.
          </Card>
          <Card
             title="API Reference"
             icon="webhook"
             color="#9935e8"
             href="/api-reference/introduction"
             arrow="true"
          >
             Integrate the API into your workflows.
          </Card>
          <Card
             title="Go to our website"
             icon="globe"
             color="#9935e8"
             href="https://enginify.in/"
             arrow="true"
          >
             To know more about us, visit our website.
          </Card>
        </CardGroup>
    </div>
</div>
